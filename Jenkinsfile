pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'building the app'
            }
        }
        stage('test') {
            steps {
                echo 'testing the app'
            }
        }
        stage('package') {
            steps {
                echo 'packaging the app'
            }
        }        
    }
}
